import { startCountdown, stepCountdown } from '../actions';
import empty from './empty';
import middleware from './countdown';

xdescribe('the countdown middleware', () => {
  it('should step every second and finally redirect', () => {

  });
});